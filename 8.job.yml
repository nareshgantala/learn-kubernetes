apiVersion: batch/v1
kind: Job
metadata:
  name: countdown
spec:
  template:
    spec:
      containers:
      - name: counter
        image: alpine:latest
        command: ["bin/sh", "-c", "for i in 5 4 3 2 1; do echo $i; sleep 1; done"]
      restartPolicy: Never